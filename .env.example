# Database Configuration
# Option 1: Use DATABASE_URL (may have IPv6 issues on Render)
# DATABASE_URL="postgresql://postgres:password@host:5432/postgres"

# Option 2: Use individual parameters (IPv4 compatible for Render)
DB_HOST=your-supabase-host
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-supabase-password
